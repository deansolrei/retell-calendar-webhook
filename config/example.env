# Example environment variables (DO NOT store secrets in the repo)
# Either provide GOOGLE_CREDS as JSON string, or GOOGLE_CREDS_BASE64 as base64 of the JSON key,
# or for LOCAL ONLY, a credentials.json file (do NOT commit it).
SECRET_TOKEN=retell_secret_2025
GOOGLE_CREDS=
GOOGLE_CREDS_BASE64=
PORT=8080